# La version Gemini est publiée depuis son dossier de build.
# Une seule règle suffit : les chemins relatifs du build (styles-*.css, main-*.js)
# se résolvent tout seuls depuis /gemini/.
/gemini/    /gemini/dist/index.html    200
